{
  "name": "argue-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws --if-present build",
    "check": "npm run -ws --if-present check",
    "test": "npm run -w argue test",
    "test:watch": "npm run -w argue test:watch",
    "ci": "npm run check && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
